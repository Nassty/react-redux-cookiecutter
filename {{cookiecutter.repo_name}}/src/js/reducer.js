import {combineReducers} from 'redux'

let global = (state, action) => {
  switch(action.type) {
    default:
      return {};
  }
}


let reducer = combineReducers({
  global
})

export default reducer;
